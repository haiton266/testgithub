#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef long double ld;

const ll NMAX = 2e5 + 1;
const ll mod = 1e9 + 7;
double c[NMAX];
double r1, r2, h;

void solve()
{
    cout << "TEst ok";
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    solve();
    return 0;
}
